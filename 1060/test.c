#include <stdio.h>

//todo
int main(void)
{
	int L_size;
	scanf("%d", &L_size);

	int num[L_size];

	for (int i = 0; i < L_size; i++)
	{
		scanf("%d", &num[i]);
	}
	// n == 3 일때
	// 1 
	// 1 <= 3 <= 4
	return 0;
}
